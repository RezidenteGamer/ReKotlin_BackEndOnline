spring.application.name=portalAcademico

# Configura��o do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/reKotlin
spring.datasource.username=postgres
spring.datasource.password=root

# Configura��o do JPA (Hibernate)
# 'update' � bom para desenvolvimento. 'validate' ou 'none' para produ��o.
spring.jpa.hibernate.ddl-auto=update
# Mostra os comandos SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
